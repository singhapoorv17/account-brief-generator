# Copy to .env.local and fill in. Never commit the real .env.local.
ORTHOGONAL_API_KEY=YOUR_ORTHOGONAL_API_KEY
# Set to "on" to run on canned sample data without spending API credits; "off" for live calls.
FALLBACK_MODE=off
